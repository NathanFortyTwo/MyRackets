<form method="post" action="{{ path('app_racket_delete', {'id': racket.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ racket.id) }}">
    <button class="btn">Delete</button>
</form>
